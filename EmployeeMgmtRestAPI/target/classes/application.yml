
server:
  port: 8222

spring:
  datasource:
    username: sa
    password: welcome
    url: jdbc:h2:mem:emp_db

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

